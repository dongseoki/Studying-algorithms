#include <string>
#include <vector>

using namespace std;

int find_unit_number(String s, index, int u_len) {

}

int solution(string s) {
    int answer = 0;
    int s_len = s.length(); // string length.. 
    int max
        for (int u_len = 1; i <= s_len / 2; u_len++) {
            // i 단위로 자르기.
            for (int index = 0; i < s.length();) {
                find_unit_number(s, index);
            }
            // [1] i단위로 최대한 자른다.
            // [2] 그 자른 것들은 잘 이어붙인다.
            // [3] 종료 조건 에 도달하면 나머지 전부다 이어붙이고 break
            // [4] 기존 max와 비교하여.그 길이를 max에 저장.
        }
    return answer;
}